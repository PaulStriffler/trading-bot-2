pandas>=2.2
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0
python-dateutil>=2.9

# Quick-backtest data source (POC, no MT5 needed)
yfinance>=0.2

# MetaTrader 5 bridge (Windows-only package; auf macOS via Wine oder remote)
MetaTrader5>=5.0.45 ; platform_system == "Windows"

# Backtest
vectorbt>=0.26

# Indicators / TA
ta>=0.11

# Dashboard backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Tests
pytest>=8.0
pytest-cov>=5.0
